package org.jetbrains.plugins.scala.metahost.trees

import org.jetbrains.plugins.scala.lang.psi.ScalaPsiElement

import scala.meta._

object TreeAdapter {
  def ideaToMeta(tree: ScalaPsiElement): Tree = {
    tree match {


      case _ =>
    }
    ???
  }
}
