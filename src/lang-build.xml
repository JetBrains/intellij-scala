<project name="Generate various scala packages for parser needs" default="rebuild.all">
  <property file="scala.properties"/>


  <path id="base.path">
    <fileset dir="${scala.home}/lib">
      <include name="**/*.jar"/>
    </fileset>
    <fileset dir="${idea.lib}">
      <include name="**/*.jar"/>
    </fileset>
    <fileset dir="${ant.lib}">
      <include name="**/*.jar"/>
    </fileset>
    <fileset dir="${project.lib}">
      <include name="**/*.class"/>
    </fileset>
  </path>

  <path id="test.path">
    <pathelement location="${scala.home}\lib\scala-library.jar"/>
    <pathelement location="${idea.lib}\junit-4.0.jar"/>
  </path>

  <target name="clear.all" description="Clears results">
    <mkdir dir="${project.lib}"/>
    <delete>
      <fileset dir="${project.lib}"/>
    </delete>
  </target>

  <target name="create.lexer" description="Makes lexer file">
    <ant dir="org/jetbrains/plugins/scala/lang/lexer/"
         antfile="build.xml" target="generate.lexer"/>
  </target>

  <target name="make.project" depends="create.lexer" description="Builds all classes">

    <mkdir dir="${project.lib}"/>
    <mkdir dir="${project.lib}/META-INF"/>

    <copy todir="${project.lib}/META-INF">
      <fileset dir="../META-INF"/>
    </copy>

    <copy todir="${project.lib}">
      <fileset dir="../resources/"/>
    </copy>

    <javac srcdir="."
           destdir="../classes/production/Scala">
      <classpath refid="base.path"/>
    </javac>

    <!--javac srcdir="../test"
           destdir="../classes/production/Scala">
      <classpath refid="base.path"/>
    </javac-->



    <!--Scala taskdef-->
    <taskdef name="scalac"
             classname="org.jetbrains.plugins.scala.util.AntScalaCompiler"
             classpath="${project.lib}">
      <classpath>
        <pathelement location="${scala.home}/lib/scala-compiler.jar"/>
        <pathelement location="${scala.home}/lib/scala-library.jar"/>
      </classpath>
    </taskdef>

    <scalac srcdir="."
            destdir="${project.lib}"
            force="changed">
      <classpath refid="base.path"/>
      <include name="**/*.scala"/>
    </scalac>

  </target>


  <target name="rebuild.all" depends="clear.all, create.lexer" description="Builds all classes">

    <echo message="Scala project basedir = ${basedir} projectlib = ${project.lib}"/>

    <mkdir dir="${project.lib}"/>
    <mkdir dir="${project.lib}/META-INF"/>

    <copy todir="${project.lib}/META-INF">
      <fileset dir="../META-INF"/>
    </copy>

    <copy todir="${project.lib}">
      <fileset dir="../resources/"/>
    </copy>

    <javac srcdir="."
           destdir="../classes/production/Scala">
      <classpath refid="base.path"/>
    </javac>

    <!--javac srcdir="../test"
           destdir="../classes/production/Scala">
      <classpath refid="base.path"/>
    </javac-->

    <!--Scala taskdef-->
    <taskdef name="scalac"
             classname="org.jetbrains.plugins.scala.util.AntScalaCompiler"
             classpath="${project.lib}">
      <classpath>
        <pathelement location="${scala.home}/lib/scala-compiler.jar"/>
        <pathelement location="${scala.home}/lib/scala-library.jar"/>
      </classpath>
    </taskdef>

    <scalac srcdir="."
            destdir="${project.lib}"
            force="changed">
      <classpath refid="base.path"/>
      <include name="**/*.scala"/>
    </scalac>

  </target>


</project>