organization

"some string"

name := "someName"
version := "SNAPSHOT"

lazy val foo = project.in(file("foo")).enablePlugins(sbt.plugins.JvmPlugin)

object Bar

null

???

Seq(
  name := "OK",
  version := "0.0.0"
)