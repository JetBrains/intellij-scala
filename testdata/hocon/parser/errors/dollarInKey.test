$key = 42
ke$y = 42
key$ = 42
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
            PsiElement(DOLLAR)('$')
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HNumber(NUMBER)
          PsiElement(UNQUOTED_CHARS)('42')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('ke')
          PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
            PsiElement(DOLLAR)('$')
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('y')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HNumber(NUMBER)
          PsiElement(UNQUOTED_CHARS)('42')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
          PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
            PsiElement(DOLLAR)('$')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HNumber(NUMBER)
          PsiElement(UNQUOTED_CHARS)('42')
