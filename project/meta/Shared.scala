package meta

/**
  * Information shared between build and actual project source.
  *
  * This needs to be in a separate directory so that it can be imported into the project without importing
  * the other build code.
  *
  * This can be replaced with code generated by sbt-buildinfo once we can build the project with sbt.
  */
object Shared {
  val sbtStructureVersion = "7.0.0+40-35fa8975"
}
