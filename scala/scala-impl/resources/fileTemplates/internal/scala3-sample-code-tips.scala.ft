#if ((${PACKAGE_NAME} && ${PACKAGE_NAME} != ""))package ${PACKAGE_NAME}

#end
#parse("File Header.java")

// ${SearchEverywhereComment1}
// ${SearchEverywhereComment2}
@main
def main(): Unit =
  val world = "world"
  // ${ShowScalaIntentionComment1}
  // ${ShowScalaIntentionComment2}
  println("Hello " + world + "!")

  // ${RunComment}
  (1 to 5).foreach { i =>
    // ${DebugComment1}
    // ${DebugComment2}
    println(s"i = $i")
  }

