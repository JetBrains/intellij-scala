#if ((${PACKAGE_NAME} && ${PACKAGE_NAME} != ""))package ${PACKAGE_NAME}

#end
#parse("File Header.java")

//TIP ${RunComment1}
// ${RunComment2}
object Main {
  def main(args: Array[String]): Unit = {
    val world = "world"
    //TIP ${ShowScalaIntentionComment1}
    // ${ShowScalaIntentionComment2}
    println("Hello " + world + "!")

    (1 to 5).foreach { i =>
      //TIP ${DebugComment1}
      // ${DebugComment2}
      println(s"i = $i")
    }
  }
}
