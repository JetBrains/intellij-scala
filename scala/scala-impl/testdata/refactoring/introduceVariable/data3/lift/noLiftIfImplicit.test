object A {
  implicit val int: Int = 0
  def test(implicit a: Int): Nothing = ???

  Seq(1).forall(<selection>test</selection>)
}
-------
object A {
  implicit val int: Int = 0
  def test(implicit a: Int): Nothing = ???

  private val value: Nothing = test
  Seq(1).forall(value)
}