# not bound comment
// not bound comment
# bound comment
# bound comment
key.more = value

# not bound comment
// not bound comment

key.more = value

# not bound comment

key.more = value

key.more = value # not bound comment
# bound comment
key.more = value

// not bound comment

# not bound comment
// not bound comment
# bound comment
# bound comment
// bound comment
include "something"
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    PsiComment(HASH_COMMENT)('# not bound comment')
    PsiWhiteSpace('\n')
    PsiComment(DOUBLE_SLASH_COMMENT)('// not bound comment')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      PsiComment(HASH_COMMENT)('# bound comment')
      PsiWhiteSpace('\n')
      PsiComment(HASH_COMMENT)('# bound comment')
      PsiWhiteSpace('\n')
      HPrefixedField(PREFIXED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiElement(PERIOD)('.')
        HValuedField(VALUED_FIELD)
          HKey(KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(UNQUOTED_CHARS)('more')
          PsiWhiteSpace(' ')
          PsiElement(EQUALS)('=')
          PsiWhiteSpace(' ')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace('\n\n')
    PsiComment(HASH_COMMENT)('# not bound comment')
    PsiWhiteSpace('\n')
    PsiComment(DOUBLE_SLASH_COMMENT)('// not bound comment')
    PsiWhiteSpace('\n\n')
    HObjectField(OBJECT_FIELD)
      HPrefixedField(PREFIXED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiElement(PERIOD)('.')
        HValuedField(VALUED_FIELD)
          HKey(KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(UNQUOTED_CHARS)('more')
          PsiWhiteSpace(' ')
          PsiElement(EQUALS)('=')
          PsiWhiteSpace(' ')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace('\n\n')
    PsiComment(HASH_COMMENT)('# not bound comment')
    PsiWhiteSpace('\n\n')
    HObjectField(OBJECT_FIELD)
      HPrefixedField(PREFIXED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiElement(PERIOD)('.')
        HValuedField(VALUED_FIELD)
          HKey(KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(UNQUOTED_CHARS)('more')
          PsiWhiteSpace(' ')
          PsiElement(EQUALS)('=')
          PsiWhiteSpace(' ')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace('\n\n')
    HObjectField(OBJECT_FIELD)
      HPrefixedField(PREFIXED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiElement(PERIOD)('.')
        HValuedField(VALUED_FIELD)
          HKey(KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(UNQUOTED_CHARS)('more')
          PsiWhiteSpace(' ')
          PsiElement(EQUALS)('=')
          PsiWhiteSpace(' ')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace(' ')
    PsiComment(HASH_COMMENT)('# not bound comment')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      PsiComment(HASH_COMMENT)('# bound comment')
      PsiWhiteSpace('\n')
      HPrefixedField(PREFIXED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiElement(PERIOD)('.')
        HValuedField(VALUED_FIELD)
          HKey(KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(UNQUOTED_CHARS)('more')
          PsiWhiteSpace(' ')
          PsiElement(EQUALS)('=')
          PsiWhiteSpace(' ')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace('\n\n')
    PsiComment(DOUBLE_SLASH_COMMENT)('// not bound comment')
    PsiWhiteSpace('\n\n')
    PsiComment(HASH_COMMENT)('# not bound comment')
    PsiWhiteSpace('\n')
    PsiComment(DOUBLE_SLASH_COMMENT)('// not bound comment')
    PsiWhiteSpace('\n')
    HInclude(INCLUDE)
      PsiComment(HASH_COMMENT)('# bound comment')
      PsiWhiteSpace('\n')
      PsiComment(HASH_COMMENT)('# bound comment')
      PsiWhiteSpace('\n')
      PsiComment(DOUBLE_SLASH_COMMENT)('// bound comment')
      PsiWhiteSpace('\n')
      PsiElement(UNQUOTED_CHARS)('include')
      PsiWhiteSpace(' ')
      HIncluded(INCLUDED)
        HIncludeTarget(INCLUDE_TARGET)
          PsiElement(QUOTED_STRING)('"something"')