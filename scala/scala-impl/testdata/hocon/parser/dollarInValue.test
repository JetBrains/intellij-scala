key = $value
key = va$lue
key = value$
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HConcatenation(CONCATENATION)
          PsiErrorElement:characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted
            PsiElement(DOLLAR)('$')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HConcatenation(CONCATENATION)
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('va')
          PsiErrorElement:characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted
            PsiElement(DOLLAR)('$')
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('lue')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('key')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HConcatenation(CONCATENATION)
          HStringValue(STRING_VALUE)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('value')
          PsiErrorElement:characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted
            PsiElement(DOLLAR)('$')
