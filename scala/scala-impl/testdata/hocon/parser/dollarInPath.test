abc = ${$path}
abc = ${pa$th}
abc = ${path$}
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('abc')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HSubstitution(SUBSTITUTION)
          PsiElement(DOLLAR)('$')
          PsiElement(SUB_LBRACE)('{')
          HPath(PATH)
            HKey(KEY)
              PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
                PsiElement(DOLLAR)('$')
              HKeyPart(KEY_PART)
                HUnquotedString(UNQUOTED_STRING)
                  PsiElement(UNQUOTED_CHARS)('path')
          PsiElement(SUB_RBRACE)('}')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('abc')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HSubstitution(SUBSTITUTION)
          PsiElement(DOLLAR)('$')
          PsiElement(SUB_LBRACE)('{')
          HPath(PATH)
            HKey(KEY)
              HKeyPart(KEY_PART)
                HUnquotedString(UNQUOTED_STRING)
                  PsiElement(UNQUOTED_CHARS)('pa')
              PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
                PsiElement(DOLLAR)('$')
              HKeyPart(KEY_PART)
                HUnquotedString(UNQUOTED_STRING)
                  PsiElement(UNQUOTED_CHARS)('th')
          PsiElement(SUB_RBRACE)('}')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HKey(KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('abc')
        PsiWhiteSpace(' ')
        PsiElement(EQUALS)('=')
        PsiWhiteSpace(' ')
        HSubstitution(SUBSTITUTION)
          PsiElement(DOLLAR)('$')
          PsiElement(SUB_LBRACE)('{')
          HPath(PATH)
            HKey(KEY)
              HKeyPart(KEY_PART)
                HUnquotedString(UNQUOTED_STRING)
                  PsiElement(UNQUOTED_CHARS)('path')
              PsiErrorElement:key must be a concatenation of unquoted, quoted or multiline strings (characters $ " { } [ ] : = , + # ` ^ ? ! @ * & \ are forbidden unquoted)
                PsiElement(DOLLAR)('$')
          PsiElement(SUB_RBRACE)('}')
