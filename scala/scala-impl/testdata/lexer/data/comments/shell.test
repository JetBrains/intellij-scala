#!/bin/sh
exec scala "$0" "$@"
!#
Console.println("Hello, world!")
argv.toList foreach Console.println
-------
ShellComment {#!/bin/sh
exec scala "$0" "$@"
!#}
white space in line {
}
identifier {Console}
. {.}
identifier {println}
( {(}
string content {"Hello, world!"}
) {)}
white space in line {
}
identifier {argv}
. {.}
identifier {toList}
white space in line { }
identifier {foreach}
white space in line { }
identifier {Console}
. {.}
identifier {println}
